const a = 8;
const b = 5;

const calc = {
  add: function (a, b) {
    return a + b
  },

  sub(a, b) {
    return a - b
  },

  Sum() {
    return add - sub
  }
}

console.log(calc.add(8, 5));
console.log(calc.sub(10, 4));
console.log(calc.sum(user.add(10, 5), user.sub(9, 5)))